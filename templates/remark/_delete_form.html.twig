<form method="post" action="{{ path('remark_delete', {'id': remark.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ remark.id) }}">
    <button class="btn">Delete</button>
</form>
